function m(message, Discord, prefix, client) {
		if(message.content.includes("https")){
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
		} else if(message.content.includes("http")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".com")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".fr")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".tk")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".xxx")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".porn")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".cl")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".ml")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".ma")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".net")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".org")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes("www.")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes("ww4.")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes("ww1.")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes("ww3.")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes("ww2.")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".dz")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".me")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".ly")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".io")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".edu")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".gov")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".mil")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".ventures")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".travel")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".today")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".tel")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".technoconsole.logy")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".sncf")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".singles")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".pro")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".post")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".plumbing")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".photography")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".name")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".museum")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".mobi")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".info")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".lighting")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".ru")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".land")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".int")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes("jobs")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".kitchen")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".land")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".holdings")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".guru")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".graphics")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".gallery")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".eus")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".equipment")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".directory")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".diamonds")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".coop")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".contractors")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".contruction")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".clothing")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".ceo")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".catholic")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".cat")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".camera")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".bzh")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".bike")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".asia")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".aero")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
	}else if(message.content.includes(".arpa")) {
			message.delete(message.author);
			message.channel.send("" ,{embed: {
					color: 0xFFFFFF,
					title: "Lien:",
					description: "La supression de lien est activer sur ce serveur !",
			}});
		}
}
module.exports = m
